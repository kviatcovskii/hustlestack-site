# 2026-07-29 — HustleStack quote mini-carousel (19h ET slot) — Instagram ERROR

## What ran
Trigger: `hustlestack-carrossel-quote-19h-et` (2nd quote post of the day, per the
1 sales + 2 quote mix in `creatorup-estrategia-engajamento-hustlestack`).

Day-of-year (America/New_York) = 210. `idx = ((210-1) % 7) + 7 = 13`.
`QUOTE_POSTS[13]` = `algorithm-rewards-consistency`.

## What was done successfully
1. Generated slides via `generate_template.py` (had to `apt-get install fonts-dejavu-extra`
   first — `DejaVuSerif-Italic.ttf` was missing from this sandbox image, unrelated to content).
2. Copied 3 PNGs to `carousels/quotes/algorithm-rewards-consistency/2026-07-29/` in this repo,
   committed and pushed to `main` (commit `77040ba`).
3. Verified all 3 `raw.githubusercontent.com` URLs return HTTP 200.
4. Called `createScheduledPost` (Metricool) once for blogId `6566436`, Instagram, autoPublish
   true, publicationDate `2026-07-29T19:11:00 America/New_York`. Post created as `PENDING`,
   then `PUBLISHING`.

## Where it stopped
Final poll of `getScheduledPosts` (post id `355628795`, uuid `-4948325248074430605`) shows:

```
"providers":[{"network":"instagram","status":"ERROR","detailedStatus":"We restrict certain
activity to protect our community. Tell us if you think we made a mistake."}]
```

**This is not isolated to this post.** The same exact `detailedStatus` string appears on
BOTH earlier posts scheduled today on the same account:
- `355416310` — 10am ET quote post (`following-through`)
- `355533459` — 2pm ET sales post (`passive-income-online`)

All 3 of today's @hustlestackhq Instagram posts failed identically. This looks like an
Instagram-side restriction/rate-limit on the connected account itself (not a per-post content
issue — three completely different images/captions/types all hit the same error). Did not
retry `createScheduledPost` (per standing instructions: never call it more than once per run,
and don't force a retry into a wall that's already rejected the account 3 times today).

## Left as-is, not deleted
The post (`355628795`) remains in Metricool in `ERROR` state — not deleted, not retried, so
the user can inspect it directly in the planner:
https://app.metricool.com/planner/calendar?blogId=6566436&openWithPostUuid=-4948325248074430605

## Recommended next step for the user
Check the Instagram account connection / restriction notice directly in Meta Business
Suite or the Instagram app for @hustlestackhq — this reads like an account-level enforcement
action, not something a content or automation change can fix.
