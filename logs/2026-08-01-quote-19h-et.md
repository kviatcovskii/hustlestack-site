# 2026-08-01 — hustlestack-carrossel-quote-19h-et — created as DRAFT, not auto-published

## Status: images generated + pushed OK, post created as DRAFT (not auto-published)

## Why not auto-published

Checked `getScheduledPosts` before publishing (blogId 6566436, today's window, America/New_York)
and found the account's two earlier posts today already in `ERROR` status:

- 10:09 ET (`hustlestack-carrossel-quote-10h-et`, item `comparison-trap`) — Instagram provider
  status `ERROR`, detailedStatus: "We restrict certain activity to protect our community. Tell us
  if you think we made a mistake."
- 14:12 ET (`hustlestack-carrossel-sales-14h-et`, ebook `passive-income-online`) — same exact
  error (see `logs/2026-08-01-sales-14h-et.md`).

Same pattern documented on 2026-07-26, 2026-07-28, 2026-07-29, 2026-07-30, 2026-07-31, and twice
already today. Per the precedent set on 2026-07-31 (`logs/2026-07-31-quote-19h-et.md`) and
`CLAUDE.md`'s "never fake success / don't force an incomplete publish" rule, this run created the
post as a **draft** instead of a third live `autoPublish: true` attempt in the same restricted
window (`draft: true, autoPublish: false`, Metricool id `356851311`,
plannerUrl: https://app.metricool.com/planner/calendar?blogId=6566436&openWithPostUuid=-4136509574296154455).
Confirmed via `getScheduledPosts` afterward: exactly 3 posts exist for today, no duplicate, new
post is `PENDING`/`draft: true` (not `ERROR`).

## What did work in this run

- `idx` calc: day-of-year 213 (America/New_York) → `((213-1) % 7) + 7 = 9` → `QUOTE_POSTS[9]` =
  `progress-over-perfection`.
- Image generation via `generate_template.py` initially failed: `DejaVuSerif-Italic.ttf` missing
  on this cloud sandbox (`fonts-dejavu-core` ships regular+bold only — same recurring environment
  issue as 2026-07-31 and 2026-08-01's sales run). Fixed with
  `apt-get install -y fonts-dejavu-extra`, then regenerated successfully. No content edited in
  `generate_template.py`.
- 3 slides copied to `carousels/quotes/progress-over-perfection/2026-08-01/`, committed and
  pushed to `hustlestack-site` main. All 3 `raw.githubusercontent.com` URLs verified HTTP 200
  before use.
- Caption written per quote-post rules (no CTA, no ebook mention, restated payoff line + explicit
  save prompt, 3 on-theme hashtags, none product/sale-related).
- Propagated the "check for today's ERROR before forcing autoPublish" rule into
  `creatorup-estrategia-engajamento-hustlestack/SKILL.md` in the same pass (see that repo's
  latest commit) — the 2026-07-31 and 2026-08-01 sales-run logs both flagged this as a follow-up
  not yet done; doing it now per `CLAUDE.md`'s "fix becomes a file" rule.

## Pattern — now 8 occurrences in 8 days, worth escalating to the user directly

This Meta restriction message has appeared on every single day since 2026-07-25 with data
(2026-07-26, 07-28, 07-29, 07-30, 07-31, and twice on 08-01 so far). It is account-level, not
tied to a specific image/caption/ebook. A user push notification was sent alongside this run
flagging that this needs their direct attention (Meta Business Suite account status check),
since documenting it in this `logs/` folder for over a week without escalating hasn't prompted a
fix.

No git push failures, no duplicate post created, no fake success reported.
