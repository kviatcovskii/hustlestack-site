# 2026-07-28 — hustlestack-carrossel-quote-19h-et — Instagram publish ERROR

## What ran successfully
- Computed rotation index: `TZ=America/New_York date +%j` = 209 → `idx = ((209-1) % 7) + 7 = 12`
- Used `QUOTE_POSTS[12]` = `showing-up-on-bad-days` (no content edited, only rendered)
- Generated slides via `generate_template.py` (had to `apt-get install -y fonts-dejavu-extra`
  again, container was missing `DejaVuSerif-Italic.ttf`/`DejaVuSerif.ttf` — environment gap,
  not a content/layout change)
- Copied 3 PNGs to `carousels/quotes/showing-up-on-bad-days/2026-07-28/` in this repo,
  committed and pushed (commit `ed5c5d1`)
- Verified all 3 raw.githubusercontent.com URLs returned HTTP 200
- Called `createScheduledPost` (Metricool, blogId 6566436) ONCE — post id 355134536,
  uuid -8896380602811727622, scheduled 2026-07-28T19:10 America/New_York, autoPublish true

## Where it stopped
Waited ~8 min past the publish time, rechecking via `getScheduledPosts` (PENDING →
PUBLISHING → ERROR). Final status:

```
"providers":[{"network":"instagram","status":"ERROR",
  "detailedStatus":"We restrict certain activity to protect our community. Tell us if you think we made a mistake."}]
```

This is the exact same error, word for word, as the other two HustleStack posts scheduled
earlier today, and as the incident logged two days ago:
- post 354913019 (10:12 ET, `boring-work` quote carousel) — same ERROR
- post 355030811 (14:12 ET, `ai-side-hustles` sales carousel) — same ERROR
- See `logs/2026-07-26-quote-19h-et.md` — identical restriction message hit all 3 posts on
  2026-07-26 too.

**This is not a new incident — it is the same account-level Instagram restriction from
2026-07-26, still unresolved 2 days later.** All 3 daily posts on @hustlestackhq have now
failed on at least 2 separate days (26th and 28th) with the identical message. This is a
persistent block, not a one-off content flag.

## Not declared as published
Per the project's standing rule, this was NOT reported as "posted" anywhere else. The
Metricool post exists (id 355134536) but Instagram rejected it — status ERROR, not
PUBLISHED/PENDING. No retry was attempted (retrying without understanding the restriction
risks making it worse, and the project rule caps `createScheduledPost` at one call per run).

## Recommended next step (for the user)
The @hustlestackhq Instagram account itself needs direct attention (app or Meta Business
Support) to find and resolve the restriction/warning notice — Metricool is only relaying
Instagram's rejection, it cannot lift the restriction. Until it's resolved at the Instagram/
Meta level, every autoPublish attempt on this account will keep failing the same way,
regardless of which trigger or content type runs.
