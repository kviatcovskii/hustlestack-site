# 2026-07-31 — HustleStack quote carousel (19h ET slot)

## Status: images generated + pushed OK, post created as DRAFT (not auto-published)

## Why not auto-published

Checked `getScheduledPosts` before publishing (blogId 6566436, today's window, America/New_York)
and found the account's two earlier posts today already in `ERROR` status:

- 10:09 ET (`hustlestack-carrossel-quote-10h-et`, item `small-wins-compound`) — Instagram provider
  status `ERROR`, detailedStatus: "We restrict certain activity to protect our community. Tell us
  if you think we made a mistake."
- 14:12 ET (`hustlestack-carrossel-sales-14h-et`, ebook `ai-side-hustles`) — same exact error.

This is Meta's own automated-activity restriction message, at the account level, returned twice in
a row today for two independent posts. Forcing a third live `autoPublish: true` attempt in the same
few hours via the same integration looked very likely to hit the same restriction again (and
potentially extend/worsen it), so per `CLAUDE.md`'s "never fake success, don't force an incomplete
publish" rule, this run created the post as a **draft** instead
(`draft: true, autoPublish: false`, Metricool id `356607971`,
plannerUrl: https://app.metricool.com/planner/calendar?blogId=6566436&openWithPostUuid=5908222438252365515).

## What did work in this run

- `idx` calc: day-of-year 212 (America/New_York) → `((212-1) % 7) + 7 = 8` → `QUOTE_POSTS[8]` =
  `time-vs-money`.
- Image generation via `generate_template.py` initially failed: `DejaVuSerif-Italic.ttf` was
  missing on this cloud sandbox (`fonts-dejavu-core` ships regular+bold only). Fixed by installing
  `fonts-dejavu-extra` (`apt-get install -y fonts-dejavu-extra`), a local package install, no repo
  change needed. Regenerated successfully after.
- 3 slides copied to `carousels/quotes/time-vs-money/2026-07-31/`, committed and pushed to
  `hustlestack-site` main. All 3 `raw.githubusercontent.com` URLs verified HTTP 200 before use.
- Caption written per quote-post rules (no CTA, no ebook mention, save-prompt + restated hook,
  3 on-theme hashtags, none product/sale-related).

## What the user should do

1. Check the Instagram app / Meta Business Suite for @hustlestackhq for any account restriction
   notice (Settings > Account Status, or a direct in-app warning). This message usually means
   Meta's automation-detection flagged posting behavior, not a content violation on either post.
2. If the account looks fine there, this may just be temporary — retry publishing tonight's draft
   manually from the Metricool planner link above once ready, or wait for the next `RemoteTrigger`
   run and let it check `getScheduledPosts` again before deciding whether to retry.
3. If this keeps repeating across multiple days, consider spacing out the 3 daily
   `RemoteTrigger` publish times further apart, or reviewing whether Metricool's own publishing
   cadence for this account looks automated/bulk to Meta.

No git push failures, no image generation failures survived after the font fix, no duplicate post
created.
