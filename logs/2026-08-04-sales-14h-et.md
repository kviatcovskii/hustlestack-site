# 2026-08-04 — hustlestack-carrossel-sales-14h-et — BLOQUEADO NO PASSO 2/6 (Metricool)

## Status: imagens geradas + pushed OK, publicação NÃO tentada (sem acesso ao Metricool)

## O que foi feito com sucesso

1. `idx` calc: `TZ=America/New_York date +%j` → dia do ano 216 → `(216-1) % 3 = 2` → ebook
   `passive-income-online` ("Passive Income Online").
2. Escrita copy nova (6 slides, AIDA, inglês, sem em dash, sem emoji) com ângulo diferente do que
   já estava no `generate_template.py` (exemplos de "template/printable/course" e "audience already
   waiting" em vez do "digital product / silver bullet" anterior). Editada só a entrada
   `passive-income-online` em `EBOOKS`, as outras 2 entradas e `QUOTE_POSTS` intactas.
3. Geração de imagem falhou na primeira tentativa: `DejaVuSerif-Italic.ttf` ausente neste sandbox
   de nuvem (mesmo problema recorrente já visto em logs anteriores desta pasta, 07-31 a 08-02).
   Dessa vez corrigido só adicionando um fallback (`LiberationSerif-Italic.ttf`, já presente no
   sistema) na lista `_first_existing` do `FONT_SERIF_ITALIC`/`FONT_SERIF_REGULAR` em
   `generate_template.py` — não altera o design (ainda serif itálico), só resolve o path quebrado
   neste ambiente. Mudança feita só localmente no `/tmp/skills-repo` desta sessão, **sem commit/push
   nesse repositório**, conforme instrução.
4. `python3 generate_template.py` rodou com sucesso, gerou os 6 slides de `passive-income-online`
   (mais os outros 2 ebooks e as 14 quotes, intocados).
5. Copiados os 6 PNGs pra `carousels/passive-income-online/2026-08-04/` neste repositório
   (`hustlestack-site`), `git add` + `git commit` + `git push origin main` — sucesso
   (commit `ea2ed7f`).
6. Confirmado via `curl -I` que as 6 URLs abaixo retornam HTTP 200:
   - https://raw.githubusercontent.com/kviatcovskii/hustlestack-site/main/carousels/passive-income-online/2026-08-04/slide_01.png
   - .../slide_02.png
   - .../slide_03.png
   - .../slide_04.png
   - .../slide_05.png
   - .../slide_06.png

## Onde travou

Passo 2 (checar duplicata via `getScheduledPosts`) e Passo 6 (`createScheduledPost`) **não puderam
ser executados**: nesta sessão o sistema reportou explicitamente que o conector Metricool requer
autorização OAuth e que a sessão é não-interativa (sem fluxo de `/mcp` disponível), então nenhuma
ferramenta do Metricool ficou carregável via `ToolSearch`.

Este é o **segundo post HustleStack do dia bloqueado exatamente no mesmo ponto**: o quote das 10h
(ver `logs/2026-08-04-hustlestack-quote-10h.md`) já tinha batido na mesma parede (`ToolSearch` sem
resultado pra nenhuma variação de query pro Metricool), embora naquele log o `ListConnectors`
mostrasse o conector como `connected: true`. Nesta execução das 14h o sistema foi mais explícito:
respondeu de saída que o Metricool "requires authentication before its tools can be used" e que a
sessão não pode completar OAuth por ser não-interativa — sugerindo que a causa é autorização
expirada/pendente do conector, não um bug de carregamento de ferramenta como nas hipóteses do log
das 10h.

## Por que não publiquei mesmo assim

Regra permanente do projeto (`CLAUDE.md`, "Nunca finja sucesso") — sem acesso às ferramentas do
Metricool não dá pra checar duplicata nem confirmar publicação depois. Publicar "no escuro" viola a
regra. Nenhuma chamada a `createScheduledPost` foi tentada.

## Estado em que fica pro próximo agente/rotina

- As 6 imagens de hoje (`passive-income-online`, 2026-08-04) já estão geradas, commitadas e
  publicamente acessíveis nas URLs acima — **não precisa gerar de novo**, só rodar os Passos 2, 6 e
  7 (checar duplicata, `createScheduledPost`, confirmar) assim que o Metricool estiver autorizado.
- Legenda (Passo 6) ainda não foi criada no Metricool, mas o texto já está definido: gancho do
  slide 1 ("Most people picture passive income as money falling from the sky. It's not.") + CTA
  "Get the full guide, link in bio." + hashtags tipo `#passiveincome #digitalproducts #sidehustle`.
- **Ação recomendada pro usuário**: reautorizar o conector Metricool (via configurações de conector
  do claude.ai, ou `/mcp` numa sessão interativa) antes da próxima execução agendada — se isso não
  for feito, o próximo disparo (quote das 19h ET hoje, e todos os disparos de amanhã) provavelmente
  bate na mesma parede.
