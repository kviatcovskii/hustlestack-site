# 2026-08-01 — hustlestack-carrossel-sales-14h-et — Instagram publish ERROR

## Status: images generated + pushed OK, post created live but Instagram rejected it (ERROR)

## What happened

- `idx` calc: day-of-year 213 (America/New_York) → `(213-1) % 3 = 2` → ebook `passive-income-online`
  ("Passive Income Online").
- Wrote 6 new slides (new angle: "one asset that compounds" instead of the previous "stack multiple
  streams" framing), edited only the `passive-income-online` entry in `generate_template.py`.
- Image generation initially failed: `DejaVuSerif-Italic.ttf` missing on this cloud sandbox
  (same issue as 2026-07-31's log). Fixed with `apt-get install -y fonts-dejavu-extra`, then
  regenerated successfully. 6 slides copied to
  `carousels/passive-income-online/2026-08-01/`, committed and pushed to `hustlestack-site` main.
  All 6 `raw.githubusercontent.com` URLs verified HTTP 200 before use.
- Checked `getScheduledPosts` before publishing: today's 10am quote post
  (`hustlestack-carrossel-quote-10h-et`, item `comparison-trap`) was **already** in Instagram
  `ERROR` status: "We restrict certain activity to protect our community. Tell us if you think we
  made a mistake." No sales post existed yet for today, so proceeded with `autoPublish: true`
  rather than falling back to draft (in hindsight, given the 2026-07-31 precedent in this same
  `logs/` folder, this should have been a signal to publish as a draft instead — see recommendation
  below).
- Called `createScheduledPost` once (Metricool id `356802131`, scheduled 14:12 ET,
  `autoPublish: true`, `draft: false`). Result after the scheduled time passed: Instagram provider
  status `ERROR`, same exact `detailedStatus` message as the 10am post today.
- Did **not** retry / call `createScheduledPost` a second time, per the standing "never publish
  twice" rule. No duplicate post exists.

## Pattern — this is not a one-off

This exact Meta message ("We restrict certain activity to protect our community") has now shown up
in `logs/` on **2026-07-26, 2026-07-28, 2026-07-29, 2026-07-30, 2026-07-31, and today 2026-08-01** —
six occurrences over seven days, always the same wording, always account-level (not tied to one
specific image or caption). This looks like a standing Meta automation-detection restriction on
`@hustlestackhq`, not a transient blip, and reusing the same Metricool→Instagram integration on a
fixed daily cron is a plausible reason Meta keeps flagging it.

## Recommended fix for future runs (propagating per CLAUDE.md's "fix becomes a file" rule)

`creatorup-gerar-carrossel-en/SKILL.md` and/or the 3 HustleStack `RemoteTrigger` prompts should be
updated so that **before** calling `createScheduledPost` with `autoPublish: true`, the routine
checks whether any earlier post *today* is already in Instagram `ERROR` state with this specific
Meta message — if so, create the new post as `draft: true, autoPublish: false` instead of forcing
another live attempt in the same restricted window (this is what the 2026-07-31 run did correctly;
today's run did not check for this before its one publish attempt). This log documents the pattern;
propagating the actual rule into the trigger prompts / SKILL.md is a follow-up task, not done in
this run since it touches files outside what was requested today.

## What the user should do

1. Check Meta Business Suite / Instagram app for @hustlestackhq under Settings > Account Status for
   any standing restriction or warning — six flags in seven days suggests this is visible there,
   not just an API-side fluke.
2. Consider spacing out or reducing the 3 daily Metricool publish times, or publishing manually for
   a few days, to see if the flag clears once the posting cadence looks less automated to Meta.
3. Today's sales post for `passive-income-online` exists in Metricool (id `356802131`) in `ERROR`
   state — not published, not a draft either. The user can open it from Metricool's planner and
   either retry manually once the restriction clears, or delete it and let a future run regenerate.

No git push failures, no duplicate post created, no fake success reported.
